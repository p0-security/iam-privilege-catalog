name: Logic Apps API connection
description: A managed API connection (Logic Apps / connector instance) that stores the configuration and authentication binding linking workflows to a backend service.
scope: MEDIUM
notes: Connections embed reusable backend credentials/OAuth tokens; secret material is exposed only via dedicated listsecrets/listconnectionkeys actions, while the resource itself supports a single integration function.
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  delete:
    risks:
      - destruction:infra
      - impact:dos
    notes: Deletes the connection, destroying the authenticated binding so dependent Logic Apps/integrations break, similar in effect to a denial of service.
  write:
    risks:
      - impact:manipulation
      - escalation:lateral
    scope: HIGH
    notes: Creating/updating the connection lets an attacker repoint the backend target and inject/rebind credentials so the connection and its consumers act under an attacker-chosen identity.
