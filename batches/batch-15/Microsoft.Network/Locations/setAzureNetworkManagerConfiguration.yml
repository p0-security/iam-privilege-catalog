name: Azure Network location operations
description: Location-scoped internal control-plane action operations of the Microsoft.Network resource provider, covering capability checks, resource-move orchestration, ownership management, Azure Network Manager configuration, load balancer control, and effective-topology reads. These are management-plane operations rather than tenant data access.
scope: HIGH
notes: Many of these are internal/undocumented platform operations; their sensitivity ranges from inert pre-flight checks to high-impact network configuration writes.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  action:
    risks:
      - impact:manipulation
      - destruction:defense
    notes: Writes Azure Network Manager configuration (connectivity and security-admin rules), letting an attacker alter cross-VNet network policy and weaken or override enforced security-admin defenses.
  read:
    risks:
      - impact:manipulation
      - destruction:defense
    notes: Held in isolation this permission authorizes deploying Network Manager configuration, letting an attacker alter centralized connectivity and override/weaken NSG-style security admin rules across managed virtual networks.
    name: Azure Network Manager configuration request
