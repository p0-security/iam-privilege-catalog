name: AKS fleet member kube-apiserver API documentation
description: The kube-apiserver swagger-ui documentation interface on an AKS fleet member cluster, exposed via the managed control-plane passthrough. It serves the interactive API documentation describing the cluster's available API surface.
scope: LOW
notes: API documentation/schema endpoints reveal the API surface but not tenant-specific resource data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Serves the Swagger UI describing the apiserver's available API operations/schema, a low-value API-surface reconnaissance aid.
