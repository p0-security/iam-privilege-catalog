name: AKS fleet member kube-apiserver API documentation
description: The kube-apiserver ui landing/redirect endpoint on an AKS fleet member cluster, exposed via the managed control-plane passthrough. It serves the API browsing interface/redirect content.
scope: LOW
notes: API documentation/schema endpoints reveal the API surface but not tenant-specific resource data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Surfaces the apiserver UI/redirect content describing the API, providing only minor reconnaissance value.
