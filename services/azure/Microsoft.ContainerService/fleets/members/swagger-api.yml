name: AKS fleet member kube-apiserver API documentation
description: The kube-apiserver swagger-api / OpenAPI specification endpoint on an AKS fleet member cluster, exposed via the managed control-plane passthrough. It returns the schema describing the cluster's available API endpoints and resource types.
scope: LOW
notes: API documentation/schema endpoints reveal the API surface but not tenant-specific resource data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Returns the OpenAPI/Swagger spec enumerating available API endpoints and resource schemas, low-value API-surface reconnaissance.
