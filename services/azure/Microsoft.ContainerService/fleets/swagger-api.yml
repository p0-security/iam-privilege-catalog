name: AKS Fleet apiserver API documentation/schema
description: The AKS Fleet apiserver Swagger/OpenAPI documentation and UI endpoints exposed as read actions through the fleets resource provider. They serve the self-describing API schema/surface.
scope: LOW
notes: Largely standard Kubernetes API documentation; exposes the available API surface but no organization-specific data or secrets.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reads the Swagger/OpenAPI schema, enabling enumeration of the apiserver's available endpoints and resource types; low-value reconnaissance.
