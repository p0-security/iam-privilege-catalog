name: AKS API server metrics endpoint
description: A Kubernetes API server metrics-reset utility endpoint exposed via the AKS managed cluster data plane.
scope: LOW
notes: Returns only operational metrics-reset status; no sensitive configuration or data is disclosed.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reading the resetMetrics endpoint exposes only operational metrics/handler status, a low-value infrastructure recon signal.
