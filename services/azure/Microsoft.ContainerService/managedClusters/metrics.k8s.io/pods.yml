name: AKS Kubernetes pod metrics
description: Per-pod resource-utilization telemetry (CPU/memory) exposed via the Kubernetes metrics.k8s.io API for an AKS managed cluster's data plane.
scope: LOW
notes: Metrics expose only non-sensitive utilization data useful for reconnaissance, not data access or control.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reading pod metrics enumerates running pods and their CPU/memory usage, a low-sensitivity infrastructure reconnaissance signal.
