name: AKS API server API-surface documentation
description: The Kubernetes API server's UI/dashboard surface exposed via the AKS managed cluster data plane.
scope: LOW
notes: Discloses the served interface/API surface of the control plane, useful for reconnaissance.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerService
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reading the ui endpoint reveals the apiserver's served interface/API surface, a low-value infrastructure recon signal.
