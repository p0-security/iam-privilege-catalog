name: Location managed API connector operations
description: The set of operations/methods exposed by a managed API connector available in a region (connector capability schema).
scope: LOW
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Read-only enumeration of the operations a managed API connector exposes, reconnaissance of connector capabilities with no data or secret exposure.
