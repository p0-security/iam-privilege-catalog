name: App Service Environment worker pool metric definitions
description: The schema/list of available metric definitions (metric names and units) for ASE worker pools.
scope: LOW
notes: Describes which metrics exist, not their values; pure monitoring metadata.
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:metadata
    notes: Returns the available metric definitions (schema) for worker pools, exposing only monitoring metadata.
