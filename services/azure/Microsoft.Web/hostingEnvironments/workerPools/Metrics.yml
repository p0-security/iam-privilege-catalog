name: App Service Environment worker pool metrics
description: Performance/utilization metric values for the worker pools backing an App Service Environment.
scope: LOW
notes: Telemetry values reveal operational behavior and sizing but no stored data or secrets.
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reads worker-pool metric values, providing low-sensitivity infrastructure utilization telemetry for reconnaissance.
