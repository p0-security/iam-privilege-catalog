name: Web App slot metric definitions
description: "The Azure Monitor metric definitions (schema: available metric names, units, dimensions) exposed for a web app slot."
scope: LOW
notes: Monitoring metadata schema only; no metric values or sensitive data.
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:metadata
    notes: Reading metric definitions enumerates the available metric schema/types for the slot, a low-sensitivity metadata discovery.
