name: Web App slot instances
description: The scaled-out worker instances backing a web app deployment slot, enumerable as instance identifiers.
scope: LOW
notes: Read-only enumeration of compute footprint (instance IDs/scale) with no data or secret exposure.
links:
  - https://azure.permissions.cloud/iam/microsoft.web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Listing the slot's worker instances inventories the underlying compute footprint for reconnaissance.
