name: Static Site metric definitions
description: The schema of monitoring metrics (metric definitions) available for an Azure Static Web App via Azure Monitor.
scope: LOW
notes: Exposes only monitoring schema/metadata, not metric values or sensitive data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  Read:
    risks:
      - discovery:metadata
    notes: Lists the available metric definitions (monitoring schema) for a Static Site, exposing only what metrics exist with no metric values or sensitive data.
