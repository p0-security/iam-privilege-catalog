name: Static Site private link resources
description: The private-link sub-resource group IDs that an Azure Static Web App exposes for private endpoint connections.
scope: LOW
notes: Read-only connectivity metadata; no data or credential content.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Web
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  Read:
    risks:
      - discovery:network
    notes: Lists the private-link group IDs the static site exposes, providing read-only connectivity metadata for private-endpoint network reconnaissance.
