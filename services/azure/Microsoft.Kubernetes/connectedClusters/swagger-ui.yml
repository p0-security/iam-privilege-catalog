name: Arc-connected cluster apiserver API-surface/introspection endpoint
description: A Kubernetes apiserver introspection endpoint on an Azure Arc-connected cluster (Swagger UI / OpenAPI schema / UI / version). It exposes the cluster's API surface, schema, or version metadata.
scope: LOW
notes: Content is largely descriptive (effectively public API documentation/version), useful chiefly for fingerprinting and endpoint enumeration; no secrets or runtime data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Kubernetes
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Serves the Swagger UI describing the cluster API surface, enabling an attacker to inventory available API resources and operations.
