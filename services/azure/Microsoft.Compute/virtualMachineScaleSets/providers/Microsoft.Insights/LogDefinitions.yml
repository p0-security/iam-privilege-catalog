name: VM Scale Set log definitions
description: The Microsoft.Insights log definitions for a Virtual Machine Scale Set enumerate the categories/types of diagnostic logs available for that scale set. It is a read-only logging-schema resource.
scope: LOW
links:
  - https://azure.permissions.cloud/iam/Microsoft.Compute
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:logs
    notes: Lists which log categories exist for the scale set; a read-only logs-schema inventory with no log content.
