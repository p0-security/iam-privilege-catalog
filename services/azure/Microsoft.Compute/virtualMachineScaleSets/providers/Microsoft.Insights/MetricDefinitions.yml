name: Virtual Machine Scale Set metric definitions
description: "The available metric definitions (monitoring/telemetry schema: metric names, units, dimensions) for an Azure Virtual Machine Scale Set."
scope: LOW
notes: Enumerates the monitoring metric schema, not the metric values or logs themselves.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Compute
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:logs
    notes: Reads which monitoring metrics/fields exist for the scale set, a telemetry schema inventory read.
