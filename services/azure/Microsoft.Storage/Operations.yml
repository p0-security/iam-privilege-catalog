name: Storage provider operations
description: The static list of REST API operations exposed by the Microsoft.Storage resource provider.
scope: PUBLIC
notes: Public API capability metadata identical across all tenants.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Storage
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Listing the provider's available operations returns static, effectively public API metadata and confers no attacker capability.
