name: Storage Blob Service Blobs
description: Blobs are the individual data objects (files) stored inside containers of an Azure Storage account's blob service. They hold an organization's unstructured production data such as documents, backups, media, and application data.
scope: HIGH
notes: Data-plane access to blobs directly exposes stored organizational data; ADLS Gen2 (hierarchical namespace) accounts also expose POSIX ownership/ACLs at the blob level.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Storage
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  delete:
    risks:
      - destruction:data
    notes: Deleting a blob destroys organizational data stored in the blob service.
  read:
    risks:
      - exfiltration:data
      - discovery:data
    notes: Returns blob content (exfiltration of stored data) or a list of blobs (enumeration of data objects), per the description 'Returns a blob or a list of blobs'.
  write:
    risks:
      - impact:manipulation
      - impact:encryption
    notes: Writing/overwriting blob content lets an attacker tamper with stored data and overwrite blobs with attacker-encrypted versions for ransomware extortion.
