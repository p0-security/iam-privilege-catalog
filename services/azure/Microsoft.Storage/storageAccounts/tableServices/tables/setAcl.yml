name: Storage Tables
description: An Azure Storage Table is a NoSQL key-attribute data store within a storage account that holds structured organizational entity data (rows).
scope: HIGH
notes: Tables are a production data store supporting a single organizational function; their entities can hold confidential application data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Storage
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  action:
    risks:
      - escalation:privilege
      - impact:manipulation
    notes: Despite the mislabeled displayName ('Merge or update table entities'), setAcl writes the table's stored access policy (signedIdentifiers) that backs SAS tokens; an attacker can define a permissive policy to grant/broaden access to the table data and alter its access-control configuration.
