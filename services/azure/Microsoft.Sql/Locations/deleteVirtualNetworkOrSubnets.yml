name: Azure SQL location-scoped Network Security Perimeter operation
description: Subscription/location-scoped Azure SQL operations covering Network Security Perimeter update notifications, virtual network rule deletion, and region availability.
scope: HIGH
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  action:
    risks:
      - destruction:defense
    notes: Deletes the virtual network rules associated to a VNet/subnet, removing network access-control restrictions that gate connectivity to SQL servers and broadening reachability.
