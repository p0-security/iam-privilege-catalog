name: SQL private endpoint connection operation result
description: An asynchronous operation result/status endpoint for Azure SQL private endpoint connection operations.
scope: LOW
notes: Purely a long-running-operation polling endpoint; returns operation status, not resource secrets or data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns only the status/result of an async private-endpoint-connection operation, an inert poll with no actionable capability in isolation.
