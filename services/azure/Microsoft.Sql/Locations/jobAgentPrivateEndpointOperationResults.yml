name: Azure SQL job agent private endpoint operation results
description: Async operation result endpoint for job-agent private-endpoint operations on Azure SQL.
scope: LOW
notes: Operation-result polling endpoints return only completion state, not resource configuration or network detail.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns only the result/completion status of a private-endpoint operation; an inert poll with no actionable capability.
