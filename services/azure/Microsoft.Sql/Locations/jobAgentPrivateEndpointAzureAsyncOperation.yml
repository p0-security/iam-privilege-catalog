name: Azure SQL job agent private endpoint async operation
description: Async operation status endpoint for job-agent private-endpoint operations on Azure SQL.
scope: LOW
notes: Operation-status polling endpoints return only progress/completion state, not resource configuration or network detail.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns only the async status of a private-endpoint operation; an inert status poll with no meaningful capability.
