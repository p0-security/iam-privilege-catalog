name: Azure SQL change LTR backup access tier operation results
description: Async operation result endpoint for changing the access tier of long-term retention backups on Azure SQL.
scope: LOW
notes: Operation-result polling endpoints return only completion state, not resource configuration or data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns only the result of a backup-access-tier change operation; an inert poll with no actionable capability.
