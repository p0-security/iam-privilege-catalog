name: Azure SQL Managed Database TDE async operation status
description: Status of in-progress asynchronous Transparent Data Encryption operations on managed databases, scoped to a region/subscription.
scope: LOW
notes: Operation-status polling endpoint; returns no sensitive data or configuration.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Sql
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Inert operation-status poll for in-progress TDE operations; grants no meaningful attacker capability in isolation.
