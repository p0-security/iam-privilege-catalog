name: Container Registry provider operation catalog
description: The static catalog of available REST API operations exposed by the Microsoft.ContainerRegistry resource provider.
scope: PUBLIC
notes: Effectively public API-surface documentation identical across all tenants.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerRegistry
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Listing the provider's available operations returns static, effectively public API metadata and confers no attacker capability.
