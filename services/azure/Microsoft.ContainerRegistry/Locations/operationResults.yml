name: Container Registry async operation results
description: The status/result of an asynchronous Microsoft.ContainerRegistry control-plane operation, returned for operation-status polling.
scope: LOW
notes: Returns only operation progress/completion state, not organizational data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.ContainerRegistry
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Polling the result of an async operation returns only operation status/completion and grants no meaningful attacker capability in isolation.
