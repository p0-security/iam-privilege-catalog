name: Internal public IP addresses
description: Internal public IP addresses are network-facing IP resources tracked at the subscription level.
scope: LOW
notes: Read-only enumeration of IP endpoints; non-sensitive network metadata.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:network
    notes: Lists internal public IP addresses in the subscription, inventorying network-facing endpoints for reconnaissance.
