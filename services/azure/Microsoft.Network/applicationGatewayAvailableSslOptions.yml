name: Application Gateway available SSL options
description: A read-only list of the SSL/TLS options, protocols, and cipher suites that Azure Application Gateway supports. This is static provider reference data, not tenant configuration.
scope: PUBLIC
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns generic platform-supported SSL options; exposes nothing tenant-specific, so no attacker capability in isolation.
