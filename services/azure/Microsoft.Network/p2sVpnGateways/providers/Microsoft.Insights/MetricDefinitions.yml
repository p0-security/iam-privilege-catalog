name: P2S VPN gateway metric definitions
description: Azure Monitor metric definitions enumerate the available monitoring metrics (schema/names/types) for a point-to-site VPN gateway.
scope: LOW
notes: Monitoring schema metadata only; contains no metric values or log content.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:metadata
    notes: Reading metric definitions only enumerates the monitoring metric schema for the P2S VPN gateway, not any actual metric data.
