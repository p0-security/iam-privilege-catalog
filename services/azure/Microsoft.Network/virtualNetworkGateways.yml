name: Virtual network gateway
description: An Azure Virtual Network Gateway provides VPN (site-to-site / point-to-site) and ExpressRoute connectivity between an Azure VNet and on-premises or remote networks. It is the production network ingress/egress edge for hybrid connectivity.
scope: HIGH
notes: A gateway is the trust boundary for a single VNet's hybrid/remote connectivity; control of it can expose or sever access to an entire private network and its hosted services.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  delete:
    risks:
      - destruction:network
      - impact:dos
    notes: Deletes the gateway, severing all VPN/ExpressRoute connectivity and causing denial of service to the connected network function.
  read:
    risks:
      - discovery:network
    notes: Reads the gateway configuration (SKU, IP config, BGP, connections), inventorying the production network edge for reconnaissance.
  write:
    risks:
      - impact:manipulation
      - escalation:network
      - impact:dos
    notes: Creates/updates the gateway, letting an attacker alter routing/VPN connectivity to extend reach into the VNet or break legitimate connectivity.
