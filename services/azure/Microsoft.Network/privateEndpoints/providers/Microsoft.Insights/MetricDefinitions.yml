name: Private Endpoint metric definitions
description: The Insights metric definitions for a Private Endpoint enumerate which monitoring metrics are available for the resource (the metric schema), not actual metric values.
scope: LOW
notes: Monitoring metadata schema; no metric values or sensitive data are returned.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:metadata
    notes: Listing available metric definitions only reveals the monitoring metadata schema for the Private Endpoint, benign low-value reconnaissance.
