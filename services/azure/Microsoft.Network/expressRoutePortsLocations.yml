name: ExpressRoute ports location
description: ExpressRoute ports locations are the static catalog of peering/provider locations where ExpressRoute Direct ports can be provisioned.
scope: LOW
notes: Generic, near-public service-availability metadata; minimal sensitivity.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:network
    notes: Lists available ExpressRoute port locations, returning generic provider connectivity-location metadata for low-value reconnaissance.
