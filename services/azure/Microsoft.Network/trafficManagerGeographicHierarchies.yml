name: Traffic Manager geographic hierarchy
description: A read-only global hierarchy of geographic regions usable with the Geographic traffic-routing method. Static provider reference data, not tenant-specific.
scope: PUBLIC
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns the static global region hierarchy; reference data tied to no tenant resource, so it is inert.
