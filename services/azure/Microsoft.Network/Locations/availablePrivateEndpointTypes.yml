name: Available Private Endpoint resource types
description: A static, region-scoped catalog listing which Azure resource types support Private Endpoints in a given location.
scope: LOW
notes: Generic platform capability metadata, not tenant resources.
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Lists which resource types generically support private endpoints in a region; benign capability enumeration revealing no tenant-specific resources.
