name: DNS Resolver Policy operation status
description: A DNS Resolver Policy operation status is the progress/state record of an asynchronous DNS Resolver Policy management operation, retrieved while polling for completion.
scope: LOW
links:
  - https://azure.permissions.cloud/iam/Microsoft.Network
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Retrieving an async operation status is an inert status poll that conveys no sensitive data or actionable capability in isolation.
