name: Log Analytics workspace query (Analytics)
description: The api/query action runs KQL search queries against the data ingested into a Log Analytics workspace, returning the actual log and telemetry records.
scope: MEDIUM
notes: Workspace logs frequently contain security events, audit trails, and sensitive operational data; querying is the primary data-access path.
links:
  - https://azure.permissions.cloud/iam/Microsoft.OperationalInsights
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  action:
    risks:
      - exfiltration:logs
      - exfiltration:data
    notes: This API-path KQL search returns ingested log/telemetry records, enabling export of potentially sensitive security, audit, and operational data.
