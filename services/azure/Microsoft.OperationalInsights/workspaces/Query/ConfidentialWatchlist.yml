name: Log Analytics ConfidentialWatchlist table
description: A Log Analytics workspace data table exposing ConfidentialWatchlist records — Microsoft Sentinel confidential watchlist contents (curated sensitive reference data such as VIP users, terminated employees, sensitive assets/IPs) that analytics rules correlate against.
scope: HIGH
notes: Explicitly marked confidential; contents are sensitive security-operations data deliberately curated for detection, so the asset is HIGH sensitivity (consistent with Microsoft.SecurityInsights/ConfidentialWatchlists/read in the catalog).
links:
  - https://azure.permissions.cloud/iam/Microsoft.OperationalInsights
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - exfiltration:data
    notes: Reads the confidential watchlist contents, directly exfiltrating curated sensitive security data useful for targeting and evasion — beyond ordinary log telemetry.
