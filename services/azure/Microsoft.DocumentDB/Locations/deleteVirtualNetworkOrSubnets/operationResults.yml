name: Cosmos DB async operation result
description: An asynchronous operation-result polling endpoint that returns only the status/progress of a prior deleteVirtualNetworkOrSubnets operation on a Cosmos DB resource.
scope: LOW
links:
  - https://azure.permissions.cloud/iam/Microsoft.DocumentDB
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Returns only transient operation-status state; grants no actionable capability in isolation.
