name: Cosmos DB collection partitions
description: The physical partition (sharding) layout of a Cosmos DB collection. Internal storage topology metadata, not the documents stored in those partitions.
scope: LOW
links:
  - https://azure.permissions.cloud/iam/Microsoft.DocumentDB
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Enumerating the physical partitions inventories the internal storage/sharding topology of the collection without returning stored data.
