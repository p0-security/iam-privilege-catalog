name: Cosmos DB keyspace throughput setting operation results
description: Represents the status/result of an asynchronous long-running operation on a Cosmos DB Cassandra-API keyspace throughput (RU/s) setting.
scope: LOW
notes: Operation-result polling endpoint returns only completion state, not the underlying resource configuration or data.
links:
  - https://azure.permissions.cloud/iam/Microsoft.DocumentDB
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Polls the status of an async keyspace throughput update; returns only operation outcome metadata with no actionable capability in isolation.
