name: Cosmos DB regional partition key range metrics
description: Per-region, per-partition-key-range operational metrics for a Cosmos DB collection. Fine-grained monitoring telemetry about key-range distribution and load, not the stored data.
scope: LOW
links:
  - https://azure.permissions.cloud/iam/Microsoft.DocumentDB
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks:
      - discovery:infra
    notes: Reading regional partition-key-range metrics exposes fine-grained operational telemetry on key distribution, infrastructure reconnaissance only.
