name: Cosmos DB Cassandra table operation results
description: The asynchronous operation-status endpoint for Cassandra table control-plane operations, exposing only the progress/result of a pending operation.
scope: LOW
notes: Status-polling endpoint; carries no stored data or configuration.
links:
  - https://azure.permissions.cloud/iam/Microsoft.DocumentDB
  - https://learn.microsoft.com/en-us/azure/role-based-access-control/resource-provider-operations
privileges:
  read:
    risks: []
    notes: Reads only the status of an asynchronous operation, an inert polling endpoint that grants no meaningful attacker capability.
